# Curated Reference Run

Created by School of AI and School of QC.

This compact evidence bundle comes from the frozen seed-42 default run generated on 2026-09-20.
It includes configuration, environment, dataset summary, circuit diagnostics, optimizer status,
thresholds, score ranges, full metrics, per-anomaly-family behavior, objective history, circuit
text, and the generated report.

Large generated files such as fitted models, prepared arrays, telemetry, and row-level
predictions are intentionally omitted. Reproduce the complete 33-file run with:

~~~bash
qaad-train --config configs/default.json
~~~

See docs/VERIFICATION.md for the release checks and docs/RESULTS.md for interpretation. This
evidence represents exact local simulation on synthetic data and is not a quantum-advantage or
production-performance claim.
